# Copy this file to .env and fill in your values

# Server Configuration
PORT=4000
NODE_ENV=development

# AI API Configuration (Choose one or more)
# Priority: HuggingFace > Gemini > OpenAI

# Option 1: HuggingFace API (FREE - Recommended! No credit card needed!)
# Get from: https://huggingface.co/settings/tokens
# Create a token with "Read" permission
HUGGINGFACE_API_KEY=your_huggingface_api_key_here

# Option 2: Google Gemini API (FREE - no credit card needed!)
# Get from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Option 3: OpenAI API (Paid - requires credit card)
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=

# CORS Configuration (frontend URL)
# For local development:
FRONTEND_URL=http://localhost:3001

# For deployment, the backend automatically allows:
# - http://localhost:3001 (dev)
# - http://localhost:3000 (dev)
# - https://smart-study-assistantt.netlify.app (Netlify)
# - https://smart-study-assistant.netlify.app (Netlify alt)
# - https://smart-study-assistant-smc5.vercel.app (Vercel)
# - https://smart-study-assistant-rho.vercel.app (Vercel)
# - Any localhost origin (for development)
# 
# Additional origins can be added via environment variables:
# VERCEL_URL=https://your-app.vercel.app
# NETLIFY_URL=https://your-app.netlify.app

# MongoDB Configuration
MONGODB_URI=

# JWT Secret (Change this in production!)
JWT_SECRET=your-secret-key-change-in-production

# Firebase Admin SDK Configuration (Required for deployment)
# Get these from: Firebase Console > Project Settings > Service Accounts > Generate New Private Key
# IMPORTANT: For deployment (Render, Vercel, etc.), you MUST use environment variables, not JSON files
FIREBASE_PROJECT_ID=prasf-3c29f
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour private key here\n-----END PRIVATE KEY-----\n"
FIREBASE_PRIVATE_KEY_ID=your_private_key_id_here
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-fbsvc@prasf-3c29f.iam.gserviceaccount.com
FIREBASE_CLIENT_ID=your_client_id_here

# Optional: For local development, you can use a file path instead
# FIREBASE_SERVICE_ACCOUNT_PATH=./path/to/service-account-key.json

